spring.application.name=StoreApplication
server.port=8081

server.instance.id=${INSTANCE_ID:1}
spring.config.import=optional:configserver:http://localhost:8888
spring.datasource.url=jdbc:postgresql://localhost:5433/store
spring.datasource.username=postgres
spring.datasource.password=zerodd
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.sql.init.mode=always

# Actuator endpoints
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

#eureka.client.service-url.defaultZone=${EUREKA_ZONE:http://localhost:8761/eureka/}

# kafka configuration (Producer)
#spring.kafka.bootstrap-servers=localhost:9092
#
#spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
#spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

#
# Spring Data REST
#
spring.data.rest.base-path=/pippo

#
# Spring Security
#
spring.security.user.name=fra
spring.security.user.password=test123